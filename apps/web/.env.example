# Backend base URL — used for both REST calls and Socket.io connections.
# Development: leave unset — Vite proxy handles /api/* → http://localhost:4000
# Production:  set to your Render backend URL (no trailing slash)
# VITE_API_URL=https://vitamina-backend.onrender.com
