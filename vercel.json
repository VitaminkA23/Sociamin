{
  "installCommand": "npm install -g pnpm@9.1.0 && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @vitamina/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
